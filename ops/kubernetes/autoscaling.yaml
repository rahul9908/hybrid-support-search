apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: support-search
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: support-search
  minReplicas: 2
  maxReplicas: 10
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: support-search
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: support-search

